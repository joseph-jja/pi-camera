install nodejs and forever

# use this command to flush buffers 
echo 3 > /proc/sys/vm/drop_caches

sudo swapoff -a
sudo update-rc.d -f dphys-swapfile remove
sudo rm /var/swap

code taken from pi_videoEMailIntruder
